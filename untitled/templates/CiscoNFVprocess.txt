


sudo lshw -C disk

sudo fdisk /dev/sdb 
 type: n
 type: 1
 type: 1   (for first sector)
 hit enter to accept default last sector
 type: w
sudo mkfs -t ext3 /dev/sdb1
sudo mkdir /media/bootstrap
sudo chown -R stack:stack /media/bootstrap   (or your user instead of stack if needed)

sudo vi /etc/fstab
	add:
	/dev/sdb1    /media/bootstrap   ext3    defaults     0        2
sudo mount -a


cd /media/bootstrap/

mkdir /media/bootstrap/config
mkdir /media/bootstrap/license
mkdir /media/bootstrap/software
mkdir /media/bootstrap/content

cp ~/bootstrap/config/* /media/bootstrap/config (assuming the bootstrap directory was already created with files on the other disk)


